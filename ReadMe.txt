Change the running times of generetion in numList.java line 5:
	TIME = ... ;

Change the size of the world in Grid.java line 5:
	SIZE = ... ;

Change the storage path in outputCSV.java line 24:
	File file = new File("E:\\altruism results/" + filename + ".csv");

Run the program, type in 6 variable according to the hint;
The results and world of each step will display on the screen;
After displaying, type in the name of the csv file, and the results can be checked in the file. 